info:
  storeName: LOJA_B

server:
  servlet:
    context-path: /api/inventory-management/v1

queue:
  subscriber:
    exchange: subs-inventory-fanout
    # Adicionando namespace para a fila da loja B
    queue: SEND_EVENT_INVENTORY_STORE_B_QUEUE
    routing-key: inventory.store.b.key

  publisher:
    exchange: core-exchange
    routing-key: order.update.to.core.store.b
    dead-letter-exchange: core-exchange-dlx
    dead-letter-routing-key: dlx-routing-key.store.b
    # Adicionando namespace para a fila DLQ da loja B
    dead-letter-queue: DLQ_INVENTORY_STORE_B_QUEUE

spring:
  application:
    name: inventory-management-loja-b

  rabbitmq:
    # Apontando para o RabbitMQ centralizado
    host: rabbitmq-central
    port: 5672
    username: guest
    password: guest
    # Configurações de acknowledgment e retries
    listener:
      simple:
        acknowledge-mode: manual
        retry:
          enabled: true
          initial-interval: 1000
          max-attempts: 3
          multiplier: 1.5

  datasource:
    url: jdbc:postgresql://db_inventory_loja2:5432/inventory_db_loja2
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres

  data:
    redis:
      host: redis-loja2
      port: 6379
      username: default
      password: default

  sql:
    init:
      mode: always

#  h2:
#    console:
#      enabled: true
#      path: /h2-console
#      settings:
#        web-allow-others: true

#  datasource:
#    url: jdbc:h2:mem:testdb
#    driver-class-name: org.h2.Driver
#    username: sa
#    password: ""


  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    defer-datasource-initialization: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics, h2-console
  endpoint:
    health:
      show-details: always

#logging:
#  level:
#    org.springframework.data.jpa: INFO
#    org.hibernate.SQL: INFO
#    org.hibernate.type.descriptor.sql.BasicBinder: INFO
